# 2026-01-01 胸+三头 19:00-20:00

杠铃卧推 ~40x10 ~50x10 60x10 60x10 60x10   # ~ 开头是热身组，不计入有效容量
哑铃卧推 20x10 20x10 20x10 20x10           # 哑铃写「单只手」的重量，不写两只合计
上斜哑铃卧推 15x10x3                        # 一个重量做三组，不用写三遍
下斜悍马机推胸 50x10 50x10 50x10@8         # @8 是这一组的 RPE
把手式蝴蝶机飞鸟 25x10 25x10 20x12         # 中途降重量就照实写，别凑成同一个数

仰卧哑铃臂屈伸 10x10 10x10 10x10
直杆绳索下压 20x10 20x10 20x10

> 这一行是整次训练的备注，会整条存进这次会话。

# ──────────────────────────────────────────────────────────
# 这份样例里的数字**全是编的、而且一律取整**（重量取整五整十、次数一律 10），
# 一眼就看得出不是谁的真实记录 —— 样例进版本库，真实训练记录不该出现在这里。
# 带小数的重量是真实读数的特征，所以这里一个都不许有。
#
# 标题行是**动作之前**那条带日期或起止时间的 #（三样都可以省；只写名字的
# 标题仅限文件第一行）。其余 # 一律当注释 —— 所以下面这一整段不会顶掉标题。
# 一条带日期的 # 没被采用时会给一条 warning，不会静默改掉日期。
# > 开头的行是整次训练的备注。
# 完整语法：./scripts/hc log --syntax
