# selfcoach

个人健身教练 / 营养师 / 健康顾问工具。**模型无关**：所有数据和分析由确定性
Python 脚本产出，模型只负责措辞。

## 开始之前必读

- `knowledge/coach/persona.md` —— 教练人格**核心**（不可选）
- `knowledge/coach/personas/<语气>.md` —— **语气层**，四选一。当前值见 `data/profile.json`
  的 `persona` 或跑 `./scripts/hc persona`。语气只换措辞，**不换任何结论**
- `knowledge/coach/safety-boundaries.md` —— 医疗边界与就医升级条件
- `data-map.md` —— 数据地图：什么数据在哪、格式、怎么录入、多久导一次

## knowledge/ 的五个区

按主题分，导航见 `knowledge/README.md`：

| 目录 | 谁读 | 放什么 |
|---|---|---|
| `coach/` | 模型 | 人格核心、四种语气、安全边界、能力矩阵 |
| `measurement/` | 模型 | 围度测量规程、负荷计量口径 |
| `movements/` | 脚本 | 动作分类、动作模式、结构平衡规则 |
| `training/` | 脚本 | 周容量参考区间、心率区间、自重系数 |
| `nutrition/` | 脚本 | 候选池、配比表、食材营养、嘌呤实测值 |

**根目录是白名单**（见 `.gitignore`）：只放行这五个区、`library/` 和 `README.md`，
其余一律忽略。所以用户把**整套现成的知识库**丢在 `knowledge/` 根上是安全的，
不会被误提交 —— 别去劝他挪走，也别替他拆分类。零散的单份资料才进 `library/`。

## 用户的私有知识库

用户自己的专业资料（教材、课程、笔记）在两个地方，都不进版本库：

- `knowledge/<某某知识库>/` —— 整套现成的资料库，保持原样
- `knowledge/library/` —— 零散单份，按主题分目录

聊到营养、训练方法、有氧、恢复这类话题时，**先看 `knowledge/library/INDEX.md`**，
它会指向各个资料库的索引。判断有没有相关资料，有就打开对应那份再回答。
不要把整个目录读一遍 —— 按 INDEX 定位。

当前收了一套「真理之弓的知识库」（186 份，减脂/营养/训练）：

- 完整索引在 `knowledge/真理之弓的知识库/知识库索引.md`，**按它定位**
- 要证据等级就去 `优质文献/`；小红书和视频讲义是二手转述，
  引用时说清是哪一份，别把科普短文说成文献结论

## 读用户的资料：什么要装依赖

**除了 PDF，常见格式都不用装东西。** `.docx` `.xlsx` `.pptx` `.epub` 都是
zip+XML，标准库 `zipfile` + `xml.etree` 就能抠出正文，不要去装
`python-docx` / `openpyxl` / `pandas`。macOS 上 `textutil -convert txt -stdout`
更省事，老式 `.doc` 只能走它。

| 要装 | 场景 |
|---|---|
| `brew install poppler` | **PDF，不装完全读不了** |
| — | 音频不解析，用户当备份留着 |

**docx 里的图片读不到** —— 讲义类 docx 常有大量截图。摘要提到的数字在正文里
找不着，多半就在图里；这时候说明「关键内容在图里，我读不到」，不要猜。

### 什么时候才提醒用户装

三个条件同时成立才打断：**① 这个问题确实需要那份资料 ② 当场读不了
③ 没有别的能读的来源**。命中了就说清「装什么命令、这份是什么、装完解锁多少份」，
让用户决定。三条纪律：

- **不自己装** —— `brew install` 是对用户系统的改动，要他点头
- **不在对话开头、不在扫索引时提** —— 只在真卡住的那一刻提
- **用户说不装也要能继续** —— 明说结论少了哪份证据，或请他自己转存一份，
  绝不假装读过

### 用户让你给某个目录建索引

触发语类似「查找 `<路径>` 的文件，写一份索引」「给这个知识库建个索引」。
**格式规范和写作纪律在 `knowledge/README.md` 的「给自己的知识库加索引」一节，
动手前先读它**，这里只讲执行顺序：

1. **扫目录**：统计份数、格式分布、子目录结构。用 `find` / `ls`，别一份份打开
2. **依赖体检**：按格式分成「当场读得了」「要装东西」「不解析」三档。
   这一步**只是分档，不要在这里打断用户** —— 见下面第 4 步
3. **抽样读**：对读得了的，实际打开提炼「这份能回答什么问题」。
   份数多就按子目录抽样，但**抽到的一定要真读**
4. **读不了的那批**：如果占比高到会让索引失去价值（比如一半是 PDF 而没装
   poppler），这时候才提一次装依赖 —— 说清「装什么命令、这批是什么、
   装完解锁多少份」。**用户说不装，照样把索引写完**，把那些标成 `⚙️` 或 `—`，
   摘要写成基于文件名和目录结构的推断**并标明是推断**
5. **落盘**：写 `<那个目录>/索引.md`，再去 `knowledge/library/INDEX.md` 加一行指过去

三条不能破的：

- **绝不给读不了的文件编摘要。** 一条编出来的摘要会让你和下一个模型都以为
  这份资料已经消化过了 —— 比没有索引更糟
- **索引里的路径必须是磁盘上的真实相对路径**，能直接拿去打开。
  原库自带的编号体系一律不要沿用
- **索引本身不进版本库**（`knowledge/` 根是白名单，`library/` 也整个忽略）。
  所以索引里可以写具体内容，但**仍然不许出现用户的身份信息** ——
  写索引不是把私人资料搬进公开目录的理由

## 事实从哪来 —— 优先级链

```
knowledge/coach/safety-boundaries.md        医疗边界，压倒一切
  > profile/health-constraints.md           硬约束：禁忌动作、代谢限制、忌口
  > profile/ 的其余文件 + data/ + knowledge/  事实层
  > knowledge/library/                      使用者信任的专业资料
  > profile/coach-journal/                  线索层，**不是事实**
  > 模型自己的通用知识
```

**事实只以 `data/`、`knowledge/`、`profile/` 里非 `*.example.*` 的文件为准**，
加上模型的通用知识。`profile/coach-journal/` 明确排除在事实之外。
冲突时明确指出，不要默默选一边。

## 教练工作日志 `profile/coach-journal/`

你是个真实的教练，教练会随手记东西。这些笔记**天然不可靠** ——
上周的判断下周可能被推翻，随口一句可能根本不算数。所以它只能做三件事：

1. 提供线索（「三周前他提过嗓子不舒服」→ 值得再问一句）
2. 提示未闭合的问题（「目标转向那条还没拍板」）
3. 提供叙事连续性（「上次说好这周开始自测腰围」）

**不能做**：当成事实引用、当成约束执行、当成数字来源。

> **日志里的数字一律不许抄。** 要用于建议的数字必须重跑 `hc` 命令。
> 否则日志会变成一个陈旧数字的污染源 —— 这是这套东西最大的失败模式。

### 怎么读

上下文里如果已经有 `<coach-journal>` 块（`scripts/coach` 启动时注入的），
**直接用，不要再跑命令**。没有的话，对话开头跑一次：

```bash
./scripts/hc journal            # 最近 14 天 + 全部未闭合的待确认
./scripts/hc journal --grep 嗓  # 聊到旧话题时才全量检索
```

永远不要通读整个日志目录 —— 跟 `knowledge/library/INDEX.md` 一个道理。

### 怎么写

聊着聊着就写，**不需要用户说任何触发词**。三档可靠性分明：

| kind | 什么时候用 |
|---|---|
| `观察` | 用户说的话、我看到的事实 |
| `判断` | 我的推断。下次必须重新验算 |
| `待确认` | 重要、需要用户拍板的。闭合前每次对话都会浮出来 |

```bash
./scripts/hc journal add --kind 判断 --topic 训练 --text "…" \
    --evidence "hc compare --date 2026-08-05"
```

改变主意时**追加一条 `--supersedes <旧ID>`，不要改旧的**。整个日志只追加不修改，
允许后一周推翻前一周 —— 价值就在能看到「当时怎么想的、后来为什么改」。

### 什么时候打断用户去确认

只有命中这五类才走确认流程，其余一律只进日志、不打断：

1. **目标变更** —— 减脂→增肌、更换衡量指标
2. **硬约束增减** —— 新伤病、新禁忌动作、新忌口、用药变化
3. **长期方案变更** —— 分化调整、训练频率、有氧安排
4. **与 profile 现有记录冲突的陈述**
5. **医疗相关的新既往史**

用户拍板后：先改 `profile/`，**旧结论降格成历史行，绝不删除**；再回来标确认。

```bash
./scripts/hc journal confirm 20260807-02 --landed "profile/personal-context.md#3-目标"
```

## 最重要的一条

**数字归脚本，措辞归你。**

吨位、组数、估算 1RM、对比结论、训练处方，全部由 `hc` 命令算出。
不要心算，不要估算，不要引入 `facts.json` 里没有的数字。

用户可能今天用 Claude、明天用 GPT。数字由脚本产出，这份健康记录才有纵向价值。

## 命令

```bash
./scripts/hc doctor                    # 环境体检
./scripts/hc sync --since 30d          # 从训记同步（训练接口 30s/天限频，会预告耗时）
./scripts/hc log                       # 手记训练（没有训记也能用）
./scripts/hc summary --date 2026-07-12 # 单次训练指标
./scripts/hc compare                   # 本次 vs 上次同部位
./scripts/hc next 胸                    # 下次训练建议
./scripts/hc report weekly             # 自包含 HTML 报告
./scripts/hc classify --unknown-only   # 看看有哪些动作没认出来
./scripts/hc dice                      # 食物骰子：今天吃什么
./scripts/hc journal                   # 教练工作日志（线索层，非事实）
./scripts/hc persona                   # 看/换教练语气（四选一，只换措辞）
./scripts/hc calib                     # 负荷口径：同一动作换机位导致的重量跳变
./scripts/hc setup                     # 一次填完：生理参数、饮食、语气、训练计划
```

## 负荷口径 `hc calib`

绳索器械是滑轮组，配重片上的数字取决于传动比。同一个动作换台机位，
标 40 kg 和标 18 kg 可以是同一个力 —— 脚本会老实按名字配对，
算出「估算 1RM ↓ 50%」。数字没算错，含义是假的。

`hc compare` 和 `hc doctor` 会自动预警（判据：**负荷跳到 ×1.7 以上或 ×0.59 以下，
而次数留在同一量级**，且两次都 ≥10 kg、相隔 ≤35 天）。命中时**摆出四个选项让用户
选，不要替他选**：

| 用户想做的 | 怎么做 |
|---|---|
| 改原始记录数据 | 去训记改，然后 `hc sync train --date X --force` |
| 只改项目内的数 | `hc calib set '<动作>' --date <日期> --ratio 0.5` |
| 忽略这次该动作的对比 | `hc calib set '<动作>' --date <日期> --ignore` |
| 确认是真实数据 | `hc calib set '<动作>' --date <日期> --confirm` |

三条纪律：

- **归一化，不要写进动作名。** `面拉（龙门2:1）` 会把同一个动作拆成两条曲线，
  纵向进步就永远看不出来了。用户 2026-08-10 明确否掉了这个方案
- **原始文件永远不改。** 折算在 `store.load_sessions()` 读取时做，
  `hc rebuild` 不会冲掉它。规则文件 `data/load-calibration.jsonl` 只追加
- **折算过的数字要说出来。** `MovementStats.calib_ratio` 不是 None 就说明这个
  重量被折算过，讲结论时提一句 —— 一个被悄悄改过的数字比一个明显错的数字危险

## 训练计划与周起始日

`data/profile.json` 的 `training` 块（`hc setup` 填）：训练频率、单次时长、
力量/有氧侧重、**周起始日**。两条纪律：

- **建议值只是默认值。** `plan.suggest()` 按目标和阶段推一份并说明理由，
  但用户填的永远优先。不要拿建议去否定用户的安排
- **`days_per_week` 是排计划用的参考值，不是考核指标。** 出计划时按它排
  （填 4 就排 4 练的分化）；**单周没练够绝不提** —— 「不勉强训练，状态不好优先补觉」
  是用户自己的原则。只有连续 6 周以上低于设定值的 70%，`hc doctor` 才会问一句
  「要不要把这个数改小」（`plan.frequency_drift()`）
- **周起始日是全局唯一口径。** 周报分桶、骰子的每周黄灯计数都走
  `plan.week_start_of()`。别处不许再切周。改它不会重写旧周报

## 食物骰子 `hc dice`

用户问「今天吃什么」「不知道吃啥」时，**跑 `hc dice`，不要自己列菜单**。
理由和吨位一样：候选池、忌口过滤、嘌呤筛选、破戒额度都是确定性的，
你临场想的菜单既不记得上周吃过什么，也不记得额度还剩几格。

**五层，顺序不能换**，越靠前越硬：

```
0 场景     现在能吃到什么              命令行参数
1 忌口/过敏 忌口按配比≥10%拦；过敏零容忍  health-constraints.md 的「## 忌口」
2 医学禁忌  异常指标推出的完全不能吃     profile.json 的 diet.medical_blocks
3 目标层    阶段决定额度和灯的权重       profile.json 的 diet.phase
4 偏好层    爱吃的加权，只调概率         profile.json 的 diet.likes
5 加权摇    蛋白密度 > 灯 > 嘌呤 > 近期
```

- **第 1 层和第 2 层不要混为一谈**。忌口是偏好，永远不变；医学禁忌是指标推出来的，
  **指标恢复就该删掉**。体检有新结果时主动提醒复核 `diet.medical_blocks`。
- 忌口改动**只改 `health-constraints.md`** 的「## 忌口」小节，别在别处再存一份。
  契约是两句：`不吃：X、Y。` 和 `过敏：Z。`。**改忌口时绝不要动过敏那一句**，
  也不要替用户断言「无过敏」—— 那是医学事实，不是默认值。
- 忌口按**配比占比**拦（阈值 10%，见 `dish-composition.json`），微量配料放行；
  **过敏零容忍**，微量也拦。两者硬度不同，不要合并。
- **破戒额度跟着 `diet.phase` 走**（减脂 1 / 维持 2 / 增肌 4），不是常数。
  用户目标或阶段变了，`diet.phase` 要跟着改 —— 这属于「长期方案变更」，
  按五类确认流程走。额度是用户自己定的上限，超了只陈述，不说教。
- **嘌呤分档只以 `knowledge/nutrition/purine-reference.json` 为准**（USDA/ODS 实测值）。
  不要凭印象说某样东西「嘌呤高」—— 那张表里有好几条是反直觉的
  （鸡胸和牛肉同档、豆腐低嘌呤、啤酒嘌呤其实很低）。
- 摇出来的结果**只是「摇过」，不是「吃过」**。别在别处把它当成饮食记录引用。
- 用户说某道菜「楼下那家做得清淡」这类个人化判断，用 `hc dice add` 存进个人池，
  同名会覆盖通用池。

## Skills

`skills/` 下有七个，入口是 `health-coach`。任何健康健身话题都从它开始。

## 千万别做

- 不要把 `.env` 里的训记 key 打印到对话、日志或报告里
- 不要把 `data/`、`profile/`、`knowledge/library/` 提交到版本库（已 gitignore）
- 不要在没有共同动作时比较两次训练的峰值负荷（器械和自由重量不可比）
- 不要把绳索器械的大幅涨跌直接报成力量变化 —— 滑轮传动比不同，同一动作换台
  机位标称重量能差一倍。看到 `hc calib` 的预警就摆出四个选项让用户选，
  **不要替他选一个**。口径见 `knowledge/measurement/load-measurement.md`
- 不要为了消预警去改 `data/training/` 里的原始记录 —— 折算走 `hc calib set --ratio`
- 不要为了「试一下」去跑 `hc setup` —— 它会真的改档案、还会往 `data/body/` 写体重。
  验证交互用 `hc setup --dry-run`
- 不要自己算年龄。真相源是 `age.py` 的 `age_at()`，它同时返回口径说明
  （只有出生年时全年可能偏大 1 岁，这一点要说出来）
- 不要在任何地方自己写 `d - timedelta(days=d.weekday())` 去切周 —— 周起始日是
  用户配置（周一/周日），唯一入口是 `plan.week_start_of()`
- 不要在 RPE 覆盖率不足时输出强度评价
- 改了解析逻辑用 `hc rebuild` 离线重算，不要重新同步（一年要 3 小时）
- 不要把 `coach-journal/` 里的数字直接拿来用 —— 重跑命令
- 不要手工编辑 `coach-journal/` 的 jsonl 去「修正」旧条目 —— 追加一条 `--supersedes`
